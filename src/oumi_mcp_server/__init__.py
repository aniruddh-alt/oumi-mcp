"""Oumi MCP Server for searching and retrieving training configs.

This package provides an MCP (Model Context Protocol) server that enables
searching through ~500 YAML configs for ML training, evaluation, and inference.

Install via: pip install oumi[mcp]
Run via: oumi-mcp
"""

__version__ = "0.1.0"
